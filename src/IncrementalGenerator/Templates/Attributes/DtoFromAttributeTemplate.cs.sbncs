// <auto-generated/>
#nullable enable

{{~ for $using in usings ~}}
using {{$using}};
{{~ end ~}}

namespace {{namespace}}
{
    [AttributeUsage(AttributeTargets.{{attribute_target}}, AllowMultiple = {{allow_multiple}})]
    internal sealed class DtoFromAttribute : Attribute
    {
        public DtoFromAttribute(Type entityType)
        {
            EntityType = entityType;
        }

        public Type EntityType { get; }
    }
}
