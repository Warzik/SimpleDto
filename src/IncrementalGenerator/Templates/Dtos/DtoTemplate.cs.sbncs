// <auto-generated/>
#nullable enable

{{~ for $using in usings ~}}
using {{$using}};
{{~ end ~}}

namespace {{namespace}}
{
	{{for $type_modifier in type_modifiers}}{{$type_modifier}} {{end}}{{if is_record}}record{{else}}class{{end}} {{name}}
	{
		{{~ for $property in properties ~}}
		public {{$property.type}} {{$property.name}} {get; {{if is_record}}init{{else}}set{{end}};}
		{{~ end ~}}
	}
}
